export const PROPOSAL_SUMMARIZE_PROMPT = (proposalName, proposalDescription, proposalAuthor, fundingRoundId) => ``.trim()

export const FEEDBACK_SUMMARIZE_PROMPT = (username, feedbackContent) => ``.trim()